import 'package:flutter/cupertino.dart';

const panePadding = EdgeInsets.all(12);

/// Used for some titles above text fields
const headlinePadding = EdgeInsets.symmetric(horizontal: 4);

const itemSpaceH = SizedBox(width: 8);
const itemSpaceV = SizedBox(height: 8);
